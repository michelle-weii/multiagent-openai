[project]
name = "multiagent-openai"
version = "0.1.0"
description = "Multi-agent frameworks using OpenAI"
requires-python = ">=3.10"
dependencies = [
    "openai",
    "networkx",
    "numpy",
    "matplotlib",
]

[tool.hatch.build.targets.wheel]
packages = ["frameworks"]
